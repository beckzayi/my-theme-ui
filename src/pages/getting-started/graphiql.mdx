import { Button } from 'theme-ui'
import GraphiQLEditor from '../../utils/graphiql/editor'
import Link from '../../utils/graphiql/link'
import queryString from '../../components/films/films-query'
import queryStringGetFilm from '../../components/films/get-film-query'

# GraphiQL

## Default GraphiQL

<GraphiQLEditor query={''} variables={null} />

<br />

<div>
  <Link query={queryString} text="All Films" />
</div>

<br />

Remember to add the `id` variable to the `QUERY VARIABLES` section in GraphiQL editor.

<div>
  <Link query={queryStringGetFilm} text="Get a Film" />

```json
{
  "id": "cj0nxmy3fga5s01148gf8iy3c"
}
```

</div>
